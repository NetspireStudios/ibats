---
import Layout from '../../layouts/Layout.astro';
import Navigation from '../../components/Navigation.tsx';
import Footer from '../../components/Footer.tsx';
---

<Layout title="Bookkeeping Services | Accurate Financial Management | IBATS Corp">
  <Navigation client:load />
  
  <!-- Enhanced Hero Section with Liquid Morphing Background -->
  <section class="relative py-20 md:py-32 overflow-hidden">
    <!-- Animated Liquid Background -->
    <div class="absolute inset-0 bg-gradient-to-br from-blue-50 to-blue-100">
      <div class="liquid-blob liquid-blob-1"></div>
      <div class="liquid-blob liquid-blob-2"></div>
      <div class="liquid-blob liquid-blob-3"></div>
    </div>

    <!-- Floating Numbers Animation -->
    <div class="floating-numbers">
      <div class="floating-number floating-number-1">$</div>
      <div class="floating-number floating-number-2">123</div>
      <div class="floating-number floating-number-3">456</div>
      <div class="floating-number floating-number-4">%</div>
      <div class="floating-number floating-number-5">789</div>
    </div>
    
    <div class="container-max relative z-10">
      <div class="grid lg:grid-cols-2 gap-8 md:gap-16 items-center min-h-[60vh]">
        
        <!-- Left Content -->
        <div class="scroll-reveal">
          <div class="inline-flex items-center px-3 py-1 md:px-4 md:py-2 bg-blue-100 rounded-full text-blue-700 text-xs md:text-sm font-medium mb-4 md:mb-6 service-badge">
            <svg class="w-3 h-3 md:w-4 md:h-4 mr-1 md:mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z"/>
            </svg>
            Professional Bookkeeping Services
          </div>
          
          <h1 class="text-2xl md:text-4xl lg:text-5xl xl:text-6xl font-bold text-gray-900 mb-4 md:mb-6 leading-tight">
            Precise
            <span class="gradient-text block liquid-text">Bookkeeping</span>
          </h1>
          
          <p class="text-base md:text-lg text-gray-600 mb-6 md:mb-8 leading-relaxed max-w-lg">
            Dependable bookkeeping services designed to enable you to concentrate on the most critical aspect—expanding your business.
          </p>

          <!-- Interactive Stats -->
          <div class="grid grid-cols-2 gap-4 md:gap-6 mb-6 md:mb-8">
            <div class="stat-card">
              <div class="text-2xl md:text-3xl font-bold text-blue-600 mb-1">99.9%</div>
              <p class="text-xs md:text-sm text-gray-600">Accuracy Rate</p>
              <div class="stat-progress" data-progress="99"></div>
            </div>
            <div class="stat-card" style="animation-delay: 0.2s;">
              <div class="text-2xl md:text-3xl font-bold text-blue-600 mb-1">24h</div>
              <p class="text-xs md:text-sm text-gray-600">Turnaround</p>
              <div class="stat-progress" data-progress="100"></div>
            </div>
          </div>

          <a href="/contact" class="btn-primary hover-glow">
            Schedule Your Free Consultation
            <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
        </div>

        <!-- Right Visual with 3D Card Effect -->
        <div class="scroll-reveal stagger-1">
          <div class="relative perspective-1000">
            <div class="card-3d">
              <div class="card-front">
                <img 
                  src="https://images.unsplash.com/photo-1454165804606-c3d57bc86b40?ixlib=rb-4.0.3&auto=format&fit=crop&w=2340&q=80" 
                  alt="Professional bookkeeper at work" 
                  class="w-full h-64 md:h-96 lg:h-[500px] object-cover rounded-3xl"
                />
                <div class="absolute inset-0 bg-gradient-to-tr from-blue-600/10 to-transparent rounded-3xl"></div>
              </div>
              
              <!-- Floating Feature Badges -->
              <div class="absolute top-4 -right-4 md:top-8 md:-right-8 bg-white rounded-xl md:rounded-2xl p-3 md:p-4 shadow-xl border border-blue-100 feature-badge">
                <div class="flex items-center gap-2 md:gap-3">
                  <div class="w-8 h-8 md:w-10 md:h-10 bg-gradient-to-br from-green-500 to-green-700 rounded-lg md:rounded-xl flex items-center justify-center pulse-icon">
                    <svg class="w-4 h-4 md:w-5 md:h-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                  </div>
                  <div>
                    <p class="text-xs md:text-sm font-semibold text-gray-900">Real-time Updates</p>
                    <p class="text-xs text-gray-600">Always Current</p>
                  </div>
                </div>
              </div>

              <div class="absolute bottom-4 -left-4 md:bottom-8 md:-left-8 bg-white rounded-xl md:rounded-2xl p-3 md:p-4 shadow-xl border border-blue-100 feature-badge" style="animation-delay: 0.5s;">
                <div class="flex items-center gap-2 md:gap-3">
                  <div class="w-8 h-8 md:w-10 md:h-10 bg-gradient-to-br from-blue-500 to-blue-700 rounded-lg md:rounded-xl flex items-center justify-center pulse-icon" style="animation-delay: 0.5s;">
                    <svg class="w-4 h-4 md:w-5 md:h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.40A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>
                    </svg>
                  </div>
                  <div>
                    <p class="text-xs md:text-sm font-semibold text-gray-900">Bank-level Security</p>
                    <p class="text-xs text-gray-600">Your Data Protected</p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Services Grid with Magnetic Cards -->
  <section class="section-padding bg-white">
    <div class="container-max">
      <div class="text-center mb-16 scroll-reveal">
        <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4">
          Our <span class="gradient-text">Bookkeeping</span> Services
        </h2>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">Comprehensive bookkeeping solutions tailored to your business needs</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-16">
        
        <!-- Daily Bookkeeping -->
        <div class="magnetic-service-card scroll-reveal">
          <div class="service-icon-container">
            <div class="service-icon gradient-bg-1">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
              </svg>
            </div>
            <div class="service-glow"></div>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-4">Daily Bookkeeping</h3>
          <p class="text-gray-600 leading-relaxed mb-6">
            We manage your day-to-day financial transactions, ensuring accurate tracking of all financial activities with real-time updates.
          </p>
          <div class="service-features">
            <div class="feature-item">Transaction Recording</div>
            <div class="feature-item">Income Tracking</div>
            <div class="feature-item">Expense Management</div>
          </div>
        </div>

        <!-- Month-End Reconciliations -->
        <div class="magnetic-service-card scroll-reveal stagger-1">
          <div class="service-icon-container">
            <div class="service-icon gradient-bg-2">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
              </svg>
            </div>
            <div class="service-glow"></div>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-4">Month-End Reconciliations</h3>
          <p class="text-gray-600 leading-relaxed mb-6">
            Comprehensive month-end reconciliations ensuring your records match bank statements with discrepancy identification.
          </p>
          <div class="service-features">
            <div class="feature-item">Bank Reconciliation</div>
            <div class="feature-item">Account Matching</div>
            <div class="feature-item">Error Detection</div>
          </div>
        </div>

        <!-- Payroll Processing -->
        <div class="magnetic-service-card scroll-reveal stagger-2">
          <div class="service-icon-container">
            <div class="service-icon gradient-bg-3">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 9V7a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2m2 4h10a2 2 0 002-2v-6a2 2 0 00-2-2H9a2 2 0 00-2 2v2a2 2 0 002 2z"></path>
              </svg>
            </div>
            <div class="service-glow"></div>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-4">Payroll Processing</h3>
          <p class="text-gray-600 leading-relaxed mb-6">
            Complete payroll management ensuring employees are paid accurately and on time with all deductions handled.
          </p>
          <div class="service-features">
            <div class="feature-item">Salary Calculations</div>
            <div class="feature-item">Tax Deductions</div>
            <div class="feature-item">Benefits Management</div>
          </div>
        </div>

        <!-- Source Deduction and HST -->
        <div class="magnetic-service-card scroll-reveal stagger-3">
          <div class="service-icon-container">
            <div class="service-icon gradient-bg-4">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
            </div>
            <div class="service-glow"></div>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-4">Source Deduction & HST</h3>
          <p class="text-gray-600 leading-relaxed mb-6">
            Accurate calculation and remittance of payroll deductions and HST to government authorities per regulations.
          </p>
          <div class="service-features">
            <div class="feature-item">Tax Compliance</div>
            <div class="feature-item">HST Management</div>
            <div class="feature-item">Government Remittance</div>
          </div>
        </div>

        <!-- Cloud-Based Solutions -->
        <div class="magnetic-service-card scroll-reveal stagger-4">
          <div class="service-icon-container">
            <div class="service-icon gradient-bg-5">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path>
              </svg>
            </div>
            <div class="service-glow"></div>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-4">Cloud-Based Solutions</h3>
          <p class="text-gray-600 leading-relaxed mb-6">
            Secure cloud-based accounting software providing access to your financial information anywhere, anytime.
          </p>
          <div class="service-features">
            <div class="feature-item">Remote Access</div>
            <div class="feature-item">Real-time Sync</div>
            <div class="feature-item">Data Security</div>
          </div>
        </div>

        <!-- Professional Support -->
        <div class="magnetic-service-card scroll-reveal stagger-5">
          <div class="service-icon-container">
            <div class="service-icon gradient-bg-6">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 5.636l-3.536 3.536m0 5.656l3.536 3.536M9.172 9.172L5.636 5.636m3.536 9.192L5.636 18.364M12 12h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <div class="service-glow"></div>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-4">Professional Support</h3>
          <p class="text-gray-600 leading-relaxed mb-6">
            Experienced team providing ongoing support and guidance for informed business financial decisions.
          </p>
          <div class="service-features">
            <div class="feature-item">Expert Guidance</div>
            <div class="feature-item">Financial Insights</div>
            <div class="feature-item">Decision Support</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Interactive Benefits Section -->
  <section class="section-padding gradient-bg">
    <div class="container-max">
      <div class="grid lg:grid-cols-2 gap-16 items-center">
        
        <!-- Left Content -->
        <div class="scroll-reveal">
          <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-8">
            Why Choose Our <span class="gradient-text">Bookkeeping</span> Services?
          </h2>
          
          <div class="space-y-6">
            <div class="benefit-item">
              <div class="benefit-icon">
                <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
              </div>
              <div>
                <h3 class="text-lg font-bold text-gray-900 mb-2">Accurate & Up-to-Date Records</h3>
                <p class="text-gray-600">Keep your financial records accurate, organized, and current with our expert bookkeeping services.</p>
              </div>
            </div>

            <div class="benefit-item" style="animation-delay: 0.1s;">
              <div class="benefit-icon">
                <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                </svg>
              </div>
              <div>
                <h3 class="text-lg font-bold text-gray-900 mb-2">Real-Time Financial Insights</h3>
                <p class="text-gray-600">Access real-time financial data to make informed business decisions when you need them most.</p>
              </div>
            </div>

            <div class="benefit-item" style="animation-delay: 0.2s;">
              <div class="benefit-icon">
                <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>
                </svg>
              </div>
              <div>
                <h3 class="text-lg font-bold text-gray-900 mb-2">Reduce Administrative Burden</h3>
                <p class="text-gray-600">Let us handle your bookkeeping so you can focus on growing your business and serving customers.</p>
              </div>
            </div>

            <div class="benefit-item" style="animation-delay: 0.3s;">
              <div class="benefit-icon">
                <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
                </svg>
              </div>
              <div>
                <h3 class="text-lg font-bold text-gray-900 mb-2">Tax Compliance</h3>
                <p class="text-gray-600">Stay compliant with tax regulations and be prepared for tax season with organized records.</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Right Interactive Dashboard -->
        <div class="scroll-reveal stagger-1">
          <div class="interactive-dashboard">
            <div class="dashboard-header">
              <h3 class="text-lg font-bold text-gray-900">Financial Dashboard</h3>
              <div class="dashboard-indicators">
                <div class="indicator active"></div>
                <div class="indicator active"></div>
                <div class="indicator"></div>
              </div>
            </div>
            
            <div class="dashboard-content">
              <div class="metric-row">
                <div class="metric">
                  <span class="metric-label">Monthly Revenue</span>
                  <span class="metric-value animate-counter" data-target="45000">$0</span>
                  <div class="metric-bar">
                    <div class="metric-progress" style="width: 85%"></div>
                  </div>
                </div>
              </div>
              
              <div class="metric-row" style="animation-delay: 0.2s;">
                <div class="metric">
                  <span class="metric-label">Expenses Tracked</span>
                  <span class="metric-value animate-counter" data-target="12500">$0</span>
                  <div class="metric-bar">
                    <div class="metric-progress" style="width: 65%"></div>
                  </div>
                </div>
              </div>
              
              <div class="metric-row" style="animation-delay: 0.4s;">
                <div class="metric">
                  <span class="metric-label">Tax Savings</span>
                  <span class="metric-value animate-counter" data-target="8750">$0</span>
                  <div class="metric-bar">
                    <div class="metric-progress" style="width: 75%"></div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Call to Action -->
  <section class="section-padding bg-gradient-to-br from-blue-600 to-blue-800 relative overflow-hidden">
    <div class="absolute inset-0">
      <div class="blob blob-cta-1"></div>
      <div class="blob blob-cta-2"></div>
    </div>
    
    <div class="container-max relative z-10 text-center">
      <div class="scroll-reveal">
        <h2 class="text-3xl lg:text-4xl font-bold text-white mb-6">Ready to Streamline Your Bookkeeping?</h2>
        <p class="text-xl text-blue-100 mb-8 max-w-2xl mx-auto">
          Start with a free consultation and discover how our expert bookkeeping services can transform your business.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/contact" class="btn-primary bg-white text-blue-700 hover:bg-blue-50">
            Get Free Consultation
          </a>
          <a href="/plans-pricing" class="btn-secondary border-white text-white hover:bg-white hover:text-blue-700">
            View Pricing Plans
          </a>
        </div>
      </div>
    </div>
  </section>

  <Footer />

  <script>
    // Enhanced Service Page Animation System
    document.addEventListener('DOMContentLoaded', function() {
      // Counter Animation for Stats
      function animateCounter(element: HTMLElement, target: number): void {
        const start = 0;
        const increment = target / 60; // 1 second animation
        let current = start;
        
        const timer = setInterval(() => {
          current += increment;
          if (current >= target) {
            current = target;
            clearInterval(timer);
          }
          
          if (element.textContent?.includes('$')) {
            element.textContent = '$' + Math.floor(current).toLocaleString();
          } else {
            element.textContent = Math.floor(current).toString() + (element.dataset.suffix || '');
          }
        }, 16);
      }

      // Scroll Reveal Animation
      const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('revealed');
            
            // Trigger counter animations
            const counters = entry.target.querySelectorAll('.animate-counter');
            counters.forEach((counter, index) => {
              setTimeout(() => {
                const targetAttr = counter.getAttribute('data-target');
                if (targetAttr && counter instanceof HTMLElement) {
                  const target = parseInt(targetAttr);
                  animateCounter(counter, target);
                }
              }, index * 200);
            });

            // Trigger progress bar animations
            const progressBars = entry.target.querySelectorAll('.stat-progress');
            progressBars.forEach((bar, index) => {
              if (bar instanceof HTMLElement) {
                setTimeout(() => {
                  const progress = bar.getAttribute('data-progress');
                  if (progress) {
                    bar.style.width = progress + '%';
                  }
                }, index * 300);
              }
            });
          }
        });
      }, observerOptions);

      document.querySelectorAll('.scroll-reveal').forEach(el => {
        observer.observe(el);
      });

      // Magnetic Service Cards Effect
      document.querySelectorAll('.magnetic-service-card').forEach(card => {
        if (card instanceof HTMLElement) {
          card.addEventListener('mousemove', (e) => {
            const rect = card.getBoundingClientRect();
            const mouseEvent = e as MouseEvent;
            const x = mouseEvent.clientX - rect.left - rect.width / 2;
            const y = mouseEvent.clientY - rect.top - rect.height / 2;
            
            card.style.transform = `translate(${x * 0.05}px, ${y * 0.05}px) rotateX(${y * 0.05}deg) rotateY(${x * 0.05}deg)`;
            
            const glow = card.querySelector('.service-glow');
            if (glow instanceof HTMLElement) {
              glow.style.opacity = '1';
              glow.style.transform = `translate(${x * 0.1}px, ${y * 0.1}px)`;
            }
          });

          card.addEventListener('mouseleave', () => {
            card.style.transform = 'translate(0px, 0px) rotateX(0deg) rotateY(0deg)';
            
            const glow = card.querySelector('.service-glow');
            if (glow instanceof HTMLElement) {
              glow.style.opacity = '0';
              glow.style.transform = 'translate(0px, 0px)';
            }
          });
        }
      });

      // 3D Card Effect
      const card3d = document.querySelector('.card-3d');
      if (card3d instanceof HTMLElement) {
        card3d.addEventListener('mousemove', (e) => {
          const rect = card3d.getBoundingClientRect();
          const mouseEvent = e as MouseEvent;
          const x = mouseEvent.clientX - rect.left - rect.width / 2;
          const y = mouseEvent.clientY - rect.top - rect.height / 2;
          
          card3d.style.transform = `rotateY(${x * 0.02}deg) rotateX(${-y * 0.02}deg)`;
        });

        card3d.addEventListener('mouseleave', () => {
          card3d.style.transform = 'rotateY(0deg) rotateX(0deg)';
        });
      }
    });
  </script>

  <style>
    /* Service Page Specific Animations */

    /* Liquid Morphing Background */
    .liquid-blob {
      position: absolute;
      border-radius: 50%;
      filter: blur(60px);
      animation: liquidMorph 20s ease-in-out infinite;
    }

    .liquid-blob-1 {
      width: 400px;
      height: 400px;
      top: 10%;
      left: 10%;
      background: linear-gradient(45deg, rgba(59, 130, 246, 0.3), rgba(147, 197, 253, 0.2));
      animation-delay: 0s;
    }

    .liquid-blob-2 {
      width: 300px;
      height: 300px;
      top: 50%;
      right: 10%;
      background: linear-gradient(45deg, rgba(37, 99, 235, 0.2), rgba(59, 130, 246, 0.3));
      animation-delay: -7s;
    }

    .liquid-blob-3 {
      width: 350px;
      height: 350px;
      bottom: 10%;
      left: 30%;
      background: linear-gradient(45deg, rgba(147, 197, 253, 0.2), rgba(37, 99, 235, 0.2));
      animation-delay: -14s;
    }

    @keyframes liquidMorph {
      0%, 100% {
        transform: translate(0px, 0px) scale(1) rotate(0deg);
        border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
      }
      25% {
        transform: translate(30px, -50px) scale(1.1) rotate(90deg);
        border-radius: 30% 60% 70% 40% / 50% 60% 30% 60%;
      }
      50% {
        transform: translate(-20px, 20px) scale(0.9) rotate(180deg);
        border-radius: 70% 30% 40% 60% / 40% 70% 60% 50%;
      }
      75% {
        transform: translate(50px, 30px) scale(1.05) rotate(270deg);
        border-radius: 40% 70% 60% 30% / 70% 50% 40% 30%;
      }
    }

    /* Floating Numbers */
    .floating-numbers {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
    }

    .floating-number {
      position: absolute;
      font-size: 2rem;
      font-weight: bold;
      color: rgba(59, 130, 246, 0.1);
      animation: numberFloat 15s infinite linear;
    }

    .floating-number-1 {
      top: 20%;
      left: 20%;
      animation-delay: 0s;
    }

    .floating-number-2 {
      top: 40%;
      right: 15%;
      animation-delay: -3s;
    }

    .floating-number-3 {
      bottom: 30%;
      left: 15%;
      animation-delay: -6s;
    }

    .floating-number-4 {
      top: 60%;
      left: 60%;
      animation-delay: -9s;
    }

    .floating-number-5 {
      bottom: 40%;
      right: 30%;
      animation-delay: -12s;
    }

    @keyframes numberFloat {
      0% {
        transform: translateY(100vh) rotate(0deg);
        opacity: 0;
      }
      10% {
        opacity: 1;
      }
      90% {
        opacity: 1;
      }
      100% {
        transform: translateY(-100px) rotate(360deg);
        opacity: 0;
      }
    }

    /* Service Badge Animation */
    .service-badge {
      animation: badgeSlide 1s ease-out;
    }

    @keyframes badgeSlide {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Liquid Text Effect */
    .liquid-text {
      animation: liquidTextWave 3s ease-in-out infinite;
    }

    @keyframes liquidTextWave {
      0%, 100% {
        transform: scale(1) skew(0deg);
      }
      50% {
        transform: scale(1.02) skew(1deg);
      }
    }

    /* Interactive Stats */
    .stat-card {
      background: white;
      padding: 1.5rem;
      border-radius: 1rem;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      position: relative;
      overflow: hidden;
      animation: statCardEntry 0.8s ease-out;
    }

    .stat-progress {
      position: absolute;
      bottom: 0;
      left: 0;
      height: 3px;
      background: linear-gradient(90deg, #3b82f6, #1d4ed8);
      width: 0%;
      transition: width 1.5s ease-out;
    }

    @keyframes statCardEntry {
      from {
        opacity: 0;
        transform: translateY(30px) scale(0.9);
      }
      to {
        opacity: 1;
        transform: translateY(0) scale(1);
      }
    }

    /* 3D Card Effect */
    .perspective-1000 {
      perspective: 1000px;
    }

    .card-3d {
      transform-style: preserve-3d;
      transition: transform 0.6s ease;
      position: relative;
    }

    /* Feature Badges */
    .feature-badge {
      animation: badgeFloat 4s ease-in-out infinite;
    }

    .pulse-icon {
      animation: iconPulse 2s ease-in-out infinite;
    }

    @keyframes iconPulse {
      0%, 100% {
        transform: scale(1);
        box-shadow: 0 0 0 0 rgba(59, 130, 246, 0.4);
      }
      50% {
        transform: scale(1.1);
        box-shadow: 0 0 0 20px rgba(59, 130, 246, 0);
      }
    }

    /* Magnetic Service Cards */
    .magnetic-service-card {
      background: white;
      padding: 2rem;
      border-radius: 1.5rem;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
      cursor: pointer;
      position: relative;
      overflow: hidden;
      transform-style: preserve-3d;
    }

    .service-icon-container {
      position: relative;
      margin-bottom: 1.5rem;
    }

    .service-icon {
      width: 4rem;
      height: 4rem;
      border-radius: 1rem;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      z-index: 2;
    }

    .service-glow {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle, rgba(59, 130, 246, 0.3), transparent);
      border-radius: 1rem;
      opacity: 0;
      transition: all 0.3s ease;
      filter: blur(20px);
    }

    .gradient-bg-1 { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); }
    .gradient-bg-2 { background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%); }
    .gradient-bg-3 { background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%); }
    .gradient-bg-4 { background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%); }
    .gradient-bg-5 { background: linear-gradient(135deg, #fa709a 0%, #fee140 100%); }
    .gradient-bg-6 { background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%); }

    /* Service Features */
    .service-features {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
    }

    .feature-item {
      background: #f1f5f9;
      color: #475569;
      font-size: 0.75rem;
      padding: 0.25rem 0.75rem;
      border-radius: 1rem;
      font-weight: 500;
    }

    /* Benefit Items */
    .benefit-item {
      display: flex;
      align-items: flex-start;
      gap: 1rem;
      opacity: 0;
      transform: translateX(-30px);
      animation: slideInFromLeft 0.8s ease-out forwards;
    }

    .benefit-icon {
      width: 3rem;
      height: 3rem;
      background: linear-gradient(135deg, #3b82f6, #1d4ed8);
      border-radius: 0.75rem;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }

    @keyframes slideInFromLeft {
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    /* Interactive Dashboard */
    .interactive-dashboard {
      background: white;
      border-radius: 1.5rem;
      padding: 2rem;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      animation: dashboardSlideUp 1s ease-out;
    }

    .dashboard-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
    }

    .dashboard-indicators {
      display: flex;
      gap: 0.5rem;
    }

    .indicator {
      width: 0.75rem;
      height: 0.75rem;
      border-radius: 50%;
      background: #e5e7eb;
      transition: background 0.3s ease;
    }

    .indicator.active {
      background: #3b82f6;
    }

    .metric-row {
      margin-bottom: 1.5rem;
      opacity: 0;
      transform: translateY(20px);
      animation: metricSlideUp 0.8s ease-out forwards;
    }

    .metric-label {
      display: block;
      font-size: 0.875rem;
      color: #6b7280;
      margin-bottom: 0.5rem;
    }

    .metric-value {
      display: block;
      font-size: 1.5rem;
      font-weight: bold;
      color: #1f2937;
      margin-bottom: 0.75rem;
    }

    .metric-bar {
      width: 100%;
      height: 0.5rem;
      background: #f3f4f6;
      border-radius: 0.25rem;
      overflow: hidden;
    }

    .metric-progress {
      height: 100%;
      background: linear-gradient(90deg, #3b82f6, #1d4ed8);
      border-radius: 0.25rem;
      transition: width 1.5s ease-out;
    }

    @keyframes metricSlideUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes dashboardSlideUp {
      from {
        opacity: 0;
        transform: translateY(40px) rotateX(10deg);
      }
      to {
        opacity: 1;
        transform: translateY(0) rotateX(0deg);
      }
    }
  </style>
</Layout> 